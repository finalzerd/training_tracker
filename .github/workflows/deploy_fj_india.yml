name: Deploy to FJ India Server
on:
  push:
    branches:
      - main
  workflow_dispatch:
  
jobs:
  run_pull:
    name: run pull
    runs-on: ubuntu-latest
    
    steps:
    - name: install ssh keys
      # check this thread to understand why its needed:
      # https://stackoverflow.com/a/70447517
      run: |
        install -m 600 -D /dev/null ~/.ssh/id_rsa
        echo "${{ secrets.FJ_INDIA_SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
        ssh-keyscan -H ${{ secrets.FJ_INDIA_SSH_HOST }} > ~/.ssh/known_hosts
    - name: connect and pull
      run: ssh ${{ secrets.FJ_INDIA_SSH_USER }}@${{ secrets.FJ_INDIA_SSH_HOST }} "cd ${{ secrets.FJ_INDIA_WORK_DIR }} && git checkout main && git pull && exit"
    - name: cleanup
      run: rm -rf ~/.ssh
